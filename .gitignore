# 依赖库（必须屏蔽）
node_modules/

# 临时测试文件（如果你不想公开的话取消这行的注释）
# test_jquery.html

# 构建输出目录（提前屏蔽，防止未来打包生成的 .exe 被误传）
dist/
out/
build/
release/

# 各种错误日志文件
npm-debug.log*
yarn-debug.log*
yarn-error.log*
*.log

# 操作系统自动生成的缓存文件
.DS_Store
Thumbs.db